<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation" style = 'margin-left:100px;'>
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>

        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-horizontal">


    <div class="form-group">
        <%= f.label :title, 'Title:', class:'control-label col-md-2 col-md-offset-1' %>
        <div class="col-md-6">
             <!-- <span class="input-group-addon">@</span> -->
            <%= f.text_field :title, class:'form-control', placeholder:'Title', required: true, autofocus: true %>

        </div>

    </div>

     <div class="form-group">
        <%= f.label :body, 'Body:', class:'control-label col-md-2 col-md-offset-1' %>
        <div class="col-md-6">
             <!-- <span class="input-group-addon">@</span> -->
            <%= f.text_area :body, class:'form-control', placeholder:'Body', required: true %>

        </div>

    </div>
  </div>

 <% label = params[:id].present? ? 'Updat post' : 'Create post' %>
 <div class="form-group">

             <div class="col-md-2 col-md-offset-3">
            <%= f.submit "#{label}", class:'btn btn-info' %>
        </div>
    </div>

  <% end %>






